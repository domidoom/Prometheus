# ── Secrets & shell dotfiles — never commit ──
.env
.env.local
.env.*.local
.bash_history
.bash_logout
.bash_profile
.bashrc
.profile
.zshrc
.zshrc_history
.fishsrv.pl
.claude.json
.claude/
.config/

# ── Caches ──
.cache/

# ── Build output ──
node_modules/
dist/
*.tsbuildinfo

# ── Runtime data (DB, sessions, ipc, per-group folders) ──
data/
logs/
tmp/
store/
groups/
home/
git/

# ── Archives / stray binaries ──
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
webcam_capture.jpg
1
EOF

# ── Nested / stray duplicate dirs ──
Warden/
warden/
attic/
jarvis-install/
jarvis-install (2)/
jarvis-install (3)/

# ── Vendored Google Fonts cache (mirrored gstatic/googleapis — re-fetchable) ──
public/fonts.gstatic.com/
public/fonts.googleapis.com/

# ── Voice app: keep only main.py + its runtime closure ──
# Flutter/Android app (separate product, not needed to run main.py)
voice/dockbox_voice/
# Windows installer / packaging artifacts
voice/*.spec
voice/build.ps1
voice/installer.iss
voice/run.ps1
voice/VOICE_APP_RUNDOWN.md
# Alternative entry points / setup utils (not needed to run main.py)
voice/linux.py
voice/login.py
voice/setup.py

# ── Python runtime ──
__pycache__/
*.pyc
.venv/
venv/

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~